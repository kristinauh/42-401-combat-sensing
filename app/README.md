# App

This folder contains the final runnable application for the project.

Workflow:
1. Flash `ble_stream.ino` to the device
2. Activate your Python environment and install dependencies (if not already done)
3. Run main.py
4. View live data in the GUI
